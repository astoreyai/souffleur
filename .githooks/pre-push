#!/usr/bin/env bash
# Pre-push gate: run the local CI before any push is allowed.
# Enable once per clone:  git config core.hooksPath .githooks
# Bypass for one push:    git push --no-verify
set -euo pipefail
exec "$(git rev-parse --show-toplevel)/scripts/ci.sh"
